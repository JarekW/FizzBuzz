#!/usr/bin/env php
<?php

require_once __DIR__ . '/../vendor/autoload.php';

$fizzBuzzGenerator = new \Example\FizzBuzzGenerator();

foreach ($fizzBuzzGenerator->generate() as $line) {
    printf("%s\n", $line);
}
